import { PartnerDashboard } from "@/components/partner/PartnerDashboard";

export const metadata = {
  title: "Partner dashboard · Grub Box",
};

export default function PartnerDashboardPage() {
  return <PartnerDashboard />;
}
